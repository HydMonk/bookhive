package com.example.bookhive.selenium;

import org.openqa.selenium.WebDriver;

public class BaseTest {
    protected WebDriver driver;
    protected String baseUrl = "http://localhost:8080"; // Change if needed
}
